# Script Review App - Environment Variables Template

# API Token for authentication (generate with: openssl rand -hex 32)
SCRIPT_REVIEW_TOKEN=change-me-in-production

# Base path for script files (defaults to output/scripts in project root)
# SCRIPTS_BASE_PATH=/path/to/esp32-project/output/scripts

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Server settings
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO
